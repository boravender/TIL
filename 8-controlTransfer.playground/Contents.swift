import UIKit

/*
 
 ** break
 
 1)반복문 2)switch문에서 사용
 문장을 중지한다
 문장이 중첩되어 있을 경우 가장 인접한 문장을 중지한다
 
 ** continue
 
 반복문에서만 사용(while문 포함)
 현재 실행중인 반복을 중지 => 다음 반복을 실행
 가장 인접한 반복문에 영향을 줌
 
 ** Labeled Statement
 
 1)반복문 2)if문 3)switch문에서 사용
 반복문이 중첩되어 있을 때 문장에 이름을 붙여 원하는 문장을 종료할 때 사용
 
 */
